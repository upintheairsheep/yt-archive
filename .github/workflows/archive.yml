on:
  schedule:
    # run every 40th minute of every hour
    - cron:  '0 14 * * 1'
  workflow_dispatch:

jobs:
  archive-channels:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        channel: [newlinersseries]
        #, , 
      fail-fast: false
    steps:
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # wget https://github.com/upintheairsheep/yt-dlp/archive/refs/heads/patch-9.zip
          # unzip patch-9.zip
          # cd yt-dlp-patch-9
          # pip install -r requirements.txt
          # python3 setup.py install
          pip install tubeup==0.0.33 yt-dlp
          mkdir -p ~/.tubeup          
          # playwright install firefox
      - uses: actions/cache@v3
        id: cache
        with:
          path: '~/.tubeup'
          key: ${{ matrix.channel }}

      - name: Download and upload channel videos
        env: 
          IA_EMAIL: ${{ secrets.IA_EMAIL }}
          IA_PASSWORD: ${{ secrets.IA_PASSWORD }}
        run: |
          ia configure --username="$IA_EMAIL" --password="$IA_PASSWORD"
          # tubeup "https://tiktok.com/@newlinersseries"  --get-comments
          yt-dlp -j --no-warnings -i --flat-playlist "https://tiktok.com/@newlinersseries" | jq -r ".id" > result.log
          tubeup https://twitch.tv/tnickelss/videos?filter=archives
          while IFS="" read -r p || [ -n "$p" ]; do
              if [ -z "$p" ]; then
                  echo "Empty URL found"
                  continue
              fi
          
              if ! tubeup https://tiktok.com/@newlinersseries/video/"$p" --debug --get-comments --use-download-archive || true; then
                  echo "Failed to archive $p"
              fi
          done < result.log
      - name: Get sha1 of .ytdlarchive
        id: sha1
        run: |
          export BRAP=$(cat ~/.tubeup/.ytdlarchive | shasum - | head -c 40)
          echo '::set-output name=sha1::$BRAP'
